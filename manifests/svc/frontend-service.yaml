apiVersion: v1
kind: Service
metadata:
  name: venkatesh-frontend-svc
spec:
  selector:
    app: venkatesh-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
